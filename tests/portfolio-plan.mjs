import {chromium} from 'playwright';
import assert from 'node:assert/strict';
const browser=await chromium.launch({headless:true});
try{const page=await browser.newPage({viewport:{width:1500,height:1100}});await page.goto('http://127.0.0.1:3100/');await page.getByRole('button',{name:'Sales',exact:true}).click();await page.getByRole('heading',{name:'Portfolio sales plan'}).waitFor();await page.getByLabel('Deals per month · whole portfolio').fill('2');await page.getByLabel('Maximum Full Cash share · % of units').fill('0');await page.getByRole('button',{name:'Cautious scenario',exact:true}).click();assert.equal(await page.locator('.sales-overload').count(),0);await page.getByRole('button',{name:'Haven Pandawa ↗',exact:true}).click();await page.getByLabel('Sales plan mode').waitFor();await page.getByRole('button',{name:'Close',exact:true}).click();await page.getByRole('button',{name:'Русский',exact:true}).click();await page.getByRole('heading',{name:'План продаж портфеля'}).waitFor();await page.screenshot({path:'/tmp/portfolio-plan.png',fullPage:true});await page.getByRole('button',{name:'Сохранить сценарий отдельной моделью'}).click();await page.getByRole('button',{name:'Финансовые модели',exact:true}).click();assert.equal(await page.locator('.model-open').count(),2);assert.match(await page.locator('.model-open').last().innerText(),/Осторожный план/);console.log('Portfolio controls, capacity, editor, RU and independent saved copy passed.')}finally{await browser.close()}
